# 리액트(Vite + TypeScript) 전환 작업계획

본 계획서는 `sta` 폴더에 퍼블리싱된 기존 퍼블리싱 웹소스(HTML, CSS, JS)를 React 프로젝트(Vite + TypeScript + styled-components)로 마이그레이션하기 위한 설계 및 단계별 작업 계획입니다.

---

## 1. 개요 및 목표

- **목적**: 기존 단일 파일 SPA 구조의 HTML/CSS/JS 소스를 유지보수 및 확장성이 우수한 React 컴포넌트 기반 애플리케이션으로 전환
- **주요 요구사항**:
  - **프레임워크**: React (Vite)
  - **언어**: TypeScript
  - **라우팅**: React Router DOM (`/` 기준 SPA 라우팅 및 뷰 전환)
  - **컴포넌트 위치**: `src/components`
  - **페이지 위치**: `src/pages`
  - **공통 컴포넌트 분리**: 헤더(Header), 푸터(Footer/Navigation) 공통 컴포넌트화
  - **CSS**: styled-components 기반 CSS-in-JS 스타일링 적용
  - **디자인 컨셉**: Aqua Minimal (맑은 수조/물방울 느낌의 톤다운 파스텔 톤 + 미니멀 여백 디자인)

---

## 2. 기술 스택 & 라이브러리

- **Build Tool**: Vite (react-ts 템플릿)
- **Framework**: React 18+
- **Language**: TypeScript
- **Styling**: `styled-components` & `styled-reset`
- **Routing**: `react-router-dom` (라우터 설정 및 1:1 DM, 프로필 등 페이지 이동 지원)
- **Icons**: Lucide React 혹은 기존 SVG/Material Symbols 기반 styled-components 컴포넌트화

---

## 3. 디렉토리 구조 (Folder Structure)

```
Mulco/
├── public/                 # 로고, 아바타 이미지 등 정적 자원 (sta/images -> public/images)
├── src/
│   ├── assets/             # 스타일 및 폰트 등 정적 자원
│   ├── components/         # 재사용 가능한 UI 공통 컴포넌트
│   │   ├── common/         # 레이아웃, 헤더, 푸터, 모달 등 공통 UI
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── Layout.tsx
│   │   │   ├── ModalOverlay.tsx
│   │   │   └── BottomSheet.tsx
│   │   └── chat/           # 채팅/분양 관련 공통 컴포넌트
│   │       ├── ChatMessage.tsx
│   │       └── MarketCard.tsx
│   ├── pages/              # 각 화면 페이지 컴포넌트
│   │   ├── LoginPage.tsx   # STEP 1: 로그인 / 가입
│   │   ├── SetupPage.tsx   # STEP 2: 동네 및 닉네임 설정
│   │   ├── MainPage.tsx    # STEP 3: 메인 단톡방 (전체, 생물분양, 용품수초 탭)
│   │   ├── ProfilePage.tsx # STEP 4: 유저 프로필 & 분양이력
│   │   └── DMPage.tsx      # STEP 5: 1:1 개인 채팅방
│   ├── styles/             # 전역 스타일 및 테마 정의
│   │   ├── GlobalStyles.ts # styled-reset 및 전역 설정
│   │   └── theme.ts        # Aqua Minimal 디자인 토큰 (CSS 변수 대응)
│   ├── types/              # TypeScript 타입 선언
│   │   └── index.ts        # User, ChatRoom, MarketItem, Review 등
│   ├── utils/              # 포맷터 및 공통 유틸 함수
│   │   └── format.ts       # priceFormatter, dateFormatter 등
│   ├── App.tsx             # 라우트 설정 및 AppState 컨텍스트 제공
│   └── main.tsx            # 진입점
├── sta/                    # [원본] 퍼블리싱 소스 폴더
└── package.json
```

---

## 4. 디자인 시스템 & 스타일 구성 (styled-components)

`src/styles/theme.ts`에 Aqua Minimal 테마 컬러를 정의하여 일관되게 적용합니다.

```typescript
export const theme = {
  colors: {
    bg: '#F9F9F7',         // 전체 배경 (오프 화이트)
    main: '#D1E6E8',       // 탭 활성, 로그인 버튼 등
    sub: '#E2EFE7',        // 분양 태그, 후기 배지 배경
    point: '#3A6073',      // CTA 단추, 강조 텍스트 등
    text: '#2C2C2C',       // 기본 차콜 그레이
    muted: '#E0E0DB',      // 연한 회색 구분선 및 안내 텍스트
    white: '#FFFFFF',
    danger: '#FF4D62',
  },
  borderRadius: {
    sm: '8px',
    md: '12px',
    lg: '24px',
    circle: '50%',
  },
  transitions: 'all 0.25s ease',
};
```

---

## 5. 데이터 상태 관리 설계 (State Management)

React Context API 혹은 전역 State Hook을 활용하여 `AppState`를 React 상태로 바인딩합니다.
- **인증 상태**: `currentUser` (localStorage 연동 자동 로그인 지원)
- **채팅방 상태**: `currentRoom`, `messages` 목록
- **마켓(분양) 데이터**: `marketItems` 목록 (분양 등록 기능으로 추가 가능)
- **DM 상태**: `dmMessages`, `activeDmTarget`

---

## 6. 단계별 전환 작업 순서 (Implementation Steps)

### **[1단계] 프로젝트 환경 구축**
1. Vite React TypeScript 프로젝트 초기 세팅
2. 필요 의존성 패키지 설치 (`styled-components`, `react-router-dom`, `styled-reset`, `@types/styled-components`)
3. `src/styles/GlobalStyles.ts`, `theme.ts` 생성 및 적용
4. `sta/images` 안의 에셋들을 `public/images`로 복사

### **[2단계] 타입 정의 및 목 데이터 전환**
1. `src/types/index.ts` 정의 (User, MarketItem, ChatMessage 등 인터페이스)
2. `sta/js/data.js`를 TypeScript 모듈 파일(`src/data/mockData.ts`)로 이관 및 타입 정의 매핑

### **[3단계] 공통 레이아웃 컴포넌트 개발**
1. 모바일 우선 레이아웃 래퍼 (`Layout.tsx`)
2. 상단 공통 헤더 (`Header.tsx`) - 페이지에 따라 뒤로가기, 타이틀, 설정 버튼 노출 여부 동적 제어
3. 하단 탭 및 입력창 연계 푸터 공통화 (`Footer.tsx` 또는 `TabBar.tsx`)
4. 공통 바텀시트 / 모달 오버레이 (`BottomSheet.tsx`, `ModalOverlay.tsx`)

### **[4단계] 페이지별 리액트 컴포넌트 마이그레이션**
1. **LoginPage**: 번호 입력 -> SMS OTP 모크 흐름 상태 컴포넌트화
2. **SetupPage**: 위치(GPS) 권한 모크 선택 및 닉네임/아바타 인디케이터 처리
3. **MainPage**: 탭 전환에 따른 하위 뷰 분리
   - [전체 채팅] 탭: 채팅 입력 및 실시간 메시지 추가 렌더링
   - [생물 분양] / [용품·수초] 탭: 마켓 카드 그리드 렌더링 + 작성 FAB 버튼 연동
4. **ProfilePage**: 신뢰도 배지 렌더링, 거래 완료 후기 팝업, 1:1 대화 연결
5. **DMPage**: 상대방 정보 확인 및 1:1 메시지 통신 모크

### **[5단계] 세부 기능 연결 & 로직 마이그레이션**
1. **분양글 등록 모달**: FAB -> PostModal 컴포넌트화 및 업로드 모크 구현
2. **이웃 신고 / 차단 모달**: 각 페이지 바텀시트 동작 연동
3. **후기 작성 팝업**: 완료 칩(Chip) 기반 멀티 선택 후기 전송 흐름 구현

### **[6단계] 검증 & 디버깅**
- 반응형 레이아웃 확인 (Max-width 430px 모바일 레이아웃)
- 페이지 전환 인터랙션 및 상태 변경 정상 동작 체크

---

## 7. 검증 계획 (Verification Plan)

### **자동/수동 검증 항목**
- [ ] **빌드 검증**: Vite production build 정상 완료 여부 (`npm run build`)
- [ ] **자동 로그인 검증**: `localStorage`에 유저 정보 저장 시 새로고침 후에도 메인 화면 자동 진입 확인
- [ ] **인증/가입 흐름**: 휴대폰 번호 -> OTP (6자리 입력) -> 동네 & 프로필 캐릭터 설정 -> 정상 메인 진입
- [ ] **실시간 인터랙션**:
  - 단톡방 채팅 입력 후 전송 시 리스트 최하단으로 스크롤 이동 및 신규 말풍선 렌더링
  - 마켓 글 등록 시 분양 탭 리스트에 실시간 반영
  - 프로필 이동 후 1:1 채팅 클릭 시 DM 화면 정상 라우팅 및 대화
- [ ] **신고 및 차단**: 신고 팝업 제출 동작 및 유저 차단 시 해당 유저의 글/채팅 가려짐 확인
